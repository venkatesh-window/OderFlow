# OrderFlow
OrderFlow is a low-latency trading engine built with Java, Spring Boot, and LMAX Disruptor. It processes buy and sell orders lock-free, matches trades in under 50 microseconds, updates Level 2 order book data, and streams real-time results via WebSockets to a React Canvas trading dashboard.
